// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("hermes", "for hermes support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("hermes", "enable hermes support", "no");

if (PHP_HERMES != "no") {
	EXTENSION("hermes", "hermes.c");
}

